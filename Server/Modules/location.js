// external
const mongoose = require('mongoose');
// internal
const Group = require('../Models/group');
const Preferences = require('../Models/preferences');
const User = require('../Models/user');
const Utils = require('../Modules/utils');
const Log = Utils.Log;
const Error = Utils.Error;

// local
var exports = module.exports = {};



// check user radius against another users radius

// check user radius against multiple users radiuses

// create group central location

    // define maximum distance based on distance to player with shortest range preferences
